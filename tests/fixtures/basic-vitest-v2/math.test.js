import { describe, it, expect } from 'vitest';

describe('Math operations (v2)', () => {
  it('adds numbers correctly', () => {
    expect(2 + 2).toBe(4);
  });

  it('multiplies numbers correctly', () => {
    expect(3 * 4).toBe(12);
  });
});
